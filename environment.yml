name: post_processing_agent_py2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=2.7
  - pytest=2.7
  - pytest-cov=1.6
  - stompest=2.1
# needed for wheel - add when switching to python3
#  - build
