name: post_processing_agent_py2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=2.7
  - pytest
  - pytest-cov
  - stompest
# needed for wheel - add when switching to python3
#  - build
