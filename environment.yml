name: post_processing_agent_py3
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.6
  - pip
  - plotly
  - pre-commit
  - pytest
  - pytest-cov
  - pytest-mock
  - requests
  - stomp.py=7
# needed for wheel - add when switching to python3
#  - build
