name: post_processing_agent
channels:
  - conda-forge
  - defaults
dependencies:
  - h5py
  - pre-commit
  - psutil
  - pytest
  - pytest-cov
  - pytest-mock
  - python>=3.10
  - python-build
  - requests
  - stomp.py=7
  - pip
  - pip:
    - git+https://github.com/neutrons/plot_publisher.git@feature/dynamic-plotly-version-injection
